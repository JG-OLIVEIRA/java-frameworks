spring:
  datasource:
    url: jdbc:mysql://localhost:3306/alura_db?allowPublicKeyRetrieval=true&useSSL=false
    username: JORGE
    password: JORGE
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
